{
  "name": "simple-webhook",
  "version": "1.0.0",
  "description": "Obsidian plugin that sends a JSON webhook when files are modified, created, deleted, or renamed.",
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "lint": "tsc --noEmit"
  },
  "keywords": [
    "obsidian",
    "plugin",
    "webhook",
    "automation"
  ],
  "author": "Jérôme Félix",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.24.0",
    "obsidian": "^1.5.0",
    "typescript": "^5.6.0"
  }
}
